# FXX — Название фичи

> Скопируйте в `docs/features/<kebab-name>.md`, замените FXX на следующий ID из FEATURES.md.

## Мета

| Поле | Значение |
|------|----------|
| ID | FXX |
| Статус | planned \| in_progress \| done \| blocked |
| Владелец | — |
| Обновлено | YYYY-MM-DD |

## Зачем

Одно-два предложения: какую боль закрывает.

## Пользовательский сценарий

1. …
2. …

## API

| Метод | Путь | Auth | Описание |
|-------|------|------|----------|
| POST | `/api/...` | Bearer / public | … |

### Контракт (пример)

```json
// request
{ }

// response 200
{ }
```

Ошибки: `400` валидация, `401` нет токена, `409` конфликт.

## UI

- Маршруты: `/...`
- Ключевые `data-testid`: …

## Данные

- Таблицы / поля: …
- Миграции: правка `src/lib/db.ts` (пока без отдельного migrator)

## Тесты

- API: `e2e/api/<name>.api.spec.ts`
- UI: `e2e/<name>.spec.ts`
- Команда: `npx playwright test e2e/api/<name>.api.spec.ts`

## Зависимости

- Блокирует: …
- Зависит от: …

## Чеклист готовности

- [ ] Код + типы
- [ ] Карточка в docs + строка в FEATURES.md
- [ ] API/UI тесты зелёные
- [ ] README / env при необходимости

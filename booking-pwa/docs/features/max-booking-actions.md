# F20 — MAX: подтвердить / отменить запись

## Мета

| Поле | Значение |
|------|----------|
| ID | F20 |
| Статус | done |
| Обновлено | 2026-08-12 |

## Сценарий

1. Новая запись → сообщение в MAX с кнопками
2. Мастер жмёт ✅ / ❌
3. Статус booking → `confirmed` / `cancelled`

Payload: `ok:{uuid}` / `no:{uuid}` (`message_callback`).
